This folder contains the working version of ShMOW that runs on my netbook (not exactly, but close).
This is the version I will first push to git.
So, whenever I make some really fucked up changes and bork the local copy I can (hopefully) restore it.
